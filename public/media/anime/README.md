# Anime covers (optional)

The tile on /experience is original artwork drawn in code
(components/InterestArt.tsx), so nothing copyrighted ships in this repo.

If you want a real image instead, drop a file here named after the entry key
from data/interests.ts and it takes over automatically:

    onepiece.jpg

Formats: .jpg .jpeg .png .webp .avif

Only use images you own or are licensed to publish. Publisher key art, cover
scans and logos are usually neither.
